/** @jsx jsx */
import { jsx } from 'theme-ui'

function Container(props) {
  return <div sx={{ maxWidth: 'maxs', mx: 'auto', px: 5, py: 8, backgroundColor: '#000000' }} {...props} />
}

export default Container
